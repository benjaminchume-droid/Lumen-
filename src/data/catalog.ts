import { MediaItem } from "../types";

/**
 * Production catalog starts empty.
 * Content is added exclusively via Lumen Forge (Gemini synthesis)
 * or real source extension adapters.
 */
export const CURATED_CATALOG: MediaItem[] = [];
