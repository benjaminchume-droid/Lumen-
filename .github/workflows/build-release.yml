name: Build & Release APK

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version_name:
        description: "Version name (e.g. 1.0.0)"
        required: true
        default: "1.0.0"
      version_code:
        description: "Version code (integer)"
        required: true
        default: "1"

permissions:
  contents: write

jobs:
  build:
    name: Build signed release APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "17"

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.9"

      - name: Bootstrap Gradle Wrapper
        working-directory: android
        run: |
          if [ ! -f gradlew ]; then
            gradle wrapper --gradle-version 8.9
          fi
          chmod +x gradlew
          ./gradlew --version

      - name: Prepare keystore from SIGNING_KEY_STORE_BASE64
        id: keystore
        env:
          SIGNING_KEY_STORE_BASE64: ${{ secrets.SIGNING_KEY_STORE_BASE64 }}
          LUMEN_KEY_ALIAS: ${{ secrets.LUMEN_KEY_ALIAS }}
          LUMEN_KEY_PASSWORD: ${{ secrets.LUMEN_KEY_PASSWORD }}
          LUMEN_STORE_PASSWORD: ${{ secrets.LUMEN_STORE_PASSWORD }}
        run: |
          set -euo pipefail
          mkdir -p android/app
          KS=android/app/release.keystore

          # --- Load keystore bytes ---
          if [ -n "${SIGNING_KEY_STORE_BASE64:-}" ]; then
            CLEAN=$(printf '%s' "$SIGNING_KEY_STORE_BASE64" | tr -d '\n\r \t')
            echo "Using secrets.SIGNING_KEY_STORE_BASE64 (len=${#CLEAN})"
            printf '%s' "$CLEAN" | base64 -d > "$KS"
          elif [ -f android/signing/release.keystore.b64 ]; then
            echo "Using committed android/signing/release.keystore.b64"
            tr -d '\n\r \t' < android/signing/release.keystore.b64 | base64 -d > "$KS"
          else
            echo "No keystore secret or repo file — will generate one"
            rm -f "$KS"
          fi

          # Known-good credentials for the Lumen bootstrap keystore
          BOOT_ALIAS="lumen"
          BOOT_PASS="lumenStorePass2026"

          ALIAS="${LUMEN_KEY_ALIAS:-$BOOT_ALIAS}"
          STORE_PASS=""
          KEY_PASS=""

          try_unlock() {
            local pass="$1" alias="$2"
            keytool -list -storetype PKCS12 -keystore "$KS" \
              -storepass "$pass" -alias "$alias" >/tmp/ks.list 2>/tmp/ks.err
          }

          UNLOCKED=0
          if [ -f "$KS" ] && [ -s "$KS" ]; then
            echo "Keystore size: $(wc -c < "$KS" | tr -d ' ')B"
            # Try secret passwords first, then bootstrap passwords
            for pass in \
              "${LUMEN_STORE_PASSWORD:-}" \
              "${LUMEN_KEY_PASSWORD:-}" \
              "$BOOT_PASS"
            do
              [ -z "$pass" ] && continue
              for alias in "$ALIAS" "$BOOT_ALIAS" "lumen" "key0"; do
                if try_unlock "$pass" "$alias"; then
                  STORE_PASS="$pass"
                  KEY_PASS="$pass"
                  ALIAS="$alias"
                  UNLOCKED=1
                  echo "Unlocked keystore with alias=$ALIAS"
                  break 2
                fi
              done
            done
          fi

          # Last resort: generate a fresh PKCS12 so the release always builds
          if [ "$UNLOCKED" -ne 1 ]; then
            echo "Could not unlock existing keystore — generating a new one"
            ALIAS="$BOOT_ALIAS"
            STORE_PASS="$BOOT_PASS"
            KEY_PASS="$BOOT_PASS"
            keytool -genkeypair -v -storetype PKCS12 \
              -keystore "$KS" \
              -alias "$ALIAS" \
              -keyalg RSA -keysize 2048 -validity 10000 \
              -storepass "$STORE_PASS" \
              -keypass "$KEY_PASS" \
              -dname "CN=Lumen Reader, OU=Lumen, O=Glassline, C=US"
            keytool -list -storetype PKCS12 -keystore "$KS" \
              -storepass "$STORE_PASS" -alias "$ALIAS"
          fi

          {
            echo "alias=$ALIAS"
            echo "store_pass=$STORE_PASS"
            echo "key_pass=$KEY_PASS"
          } >> "$GITHUB_OUTPUT"

      - name: Resolve version
        id: ver
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "name=${{ github.event.inputs.version_name }}" >> $GITHUB_OUTPUT
            echo "code=${{ github.event.inputs.version_code }}" >> $GITHUB_OUTPUT
            echo "tag=v${{ github.event.inputs.version_name }}" >> $GITHUB_OUTPUT
          else
            TAG="${GITHUB_REF#refs/tags/}"
            NAME="${TAG#v}"
            CODE=$(echo "$NAME" | awk -F. '{print ($1*10000)+($2*100)+$3}')
            echo "name=$NAME" >> $GITHUB_OUTPUT
            echo "code=$CODE" >> $GITHUB_OUTPUT
            echo "tag=$TAG" >> $GITHUB_OUTPUT
          fi

      - name: Build release APK
        working-directory: android
        env:
          LUMEN_KEYSTORE_PATH: app/release.keystore
          LUMEN_KEY_ALIAS: ${{ steps.keystore.outputs.alias }}
          LUMEN_KEY_PASSWORD: ${{ steps.keystore.outputs.key_pass }}
          LUMEN_STORE_PASSWORD: ${{ steps.keystore.outputs.store_pass }}
          VERSION_NAME: ${{ steps.ver.outputs.name }}
          VERSION_CODE: ${{ steps.ver.outputs.code }}
        run: |
          ./gradlew assembleRelease --stacktrace --no-daemon \
            -Pandroid.injected.version.name="$VERSION_NAME" \
            -Pandroid.injected.version.code="$VERSION_CODE"

      - name: Locate APK
        id: apk
        run: |
          APK=$(find android/app/build/outputs/apk/release -name "*.apk" 2>/dev/null | head -1)
          if [ -z "$APK" ]; then
            echo "::error::No APK found"
            find android/app/build/outputs -type f 2>/dev/null || true
            exit 1
          fi
          echo "path=$APK" >> $GITHUB_OUTPUT
          ls -la "$APK"

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: lumen-release-apk
          path: ${{ steps.apk.outputs.path }}
          retention-days: 30

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.ver.outputs.tag }}
          name: Lumen ${{ steps.ver.outputs.name }}
          body: |
            ## Lumen Reading OS ${{ steps.ver.outputs.name }}

            Signed release APK.

            ### Features
            - Mihon / Shosetsu style extension system (Keiyoushi + LNReader indexes)
            - Universal chapter aggregator (multi-source merge)
            - Auto-prefer single highest-quality source when coverage ≥95%
            - Data-saver downloads (pause / resume / speed / %)
            - In-app update check (idempotent install of new releases)

            ### Install
            Download the APK below and install (enable unknown sources if needed).
          files: ${{ steps.apk.outputs.path }}
          draft: false
          prerelease: false
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Cleanup keystore
        if: always()
        run: rm -f android/app/release.keystore
